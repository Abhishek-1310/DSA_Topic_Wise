494. Target Sum
You are given an integer array nums and an integer target.

You want to build an expression out of nums by adding one of the symbols '+' and '-' before each integer in nums and then concatenate all the integers.

For example, if nums = [2, 1], you can add a '+' before 2 and a '-' before 1 and concatenate them to build the expression "+2-1".
Return the number of different expressions that you can build, which evaluates to target.


Example 1:

Input: nums = [1,1,1,1,1], target = 3
Output: 5
Explanation: There are 5 ways to assign symbols to make the sum of nums be target 3.
-1 + 1 + 1 + 1 + 1 = 3
+1 - 1 + 1 + 1 + 1 = 3
+1 + 1 - 1 + 1 + 1 = 3
+1 + 1 + 1 - 1 + 1 = 3
+1 + 1 + 1 + 1 - 1 = 3
Example 2:

Input: nums = [1], target = 1
Output: 1

notes:- 
he DP part is correct now. 👍 The error is coming from this condition:
if(total < target) return 0;
You need to use absolute value because target can be negative.
For example:

nums = [1]
target = -2
total = 1
Your check:
total < target
1 < -2  // false
So you continue and calculate:
tar = (1 + (-2)) / 2
    = -0.5
Then:
new Array(-0.5 + 1)
becomes an invalid array length → RangeError.


notes2:- why this if ((total + target) % 2 !== 0) return 0;


2 × S1 = total + target
S1 = (total + target) / 2
So total + target must be even. else partition can't be done
If it's odd:
(total + target) / 2
will not be an integer, so no valid partition exists.

notes3:- why dp[sum - num]:- dp[sum] stores: The number of ways to select elements from the numbers processed so far whose sum is exactly sum.

dp[sum] += dp[sum - num];

sum 7 and current num is 3  
when u are making dp[sum] to make dp[7]  if you take num 3 how much remaing to make sum 
to make 7 if you already done 3 need 4 7-3 = 4 and check how many way we can make 4 

if we have how many way can make 3 and 4 we can get 7 


For example:
nums = [1, 1, 2]
Initially:
dp[0] = 1
Meaning: there is 1 way to make sum 0 → select nothing.

After processing the first 1:

dp[0] = 1
dp[1] = 1

After processing the second 1:

dp[0] = 1
dp[1] = 2
dp[2] = 1

Why dp[2] = 1?

Because we can choose:
1(first) + 1(second) = 2
Then process 2:
dp[2] += dp[0]
       += 1
       = 2
Now there are 2 ways to make sum 2:

1 + 1
2
So remember this
dp[sum]
does NOT store the sum itself.
It stores:
HOW MANY WAYS can I make this sum?
And: dp[sum - num]

means: How many ways can I make the remaining sum before adding num?
