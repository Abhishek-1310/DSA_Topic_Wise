104. Maximum Depth of Binary Tree
Given the root of a binary tree, return its maximum depth.
A binary tree's maximum depth is the number of nodes along the longest path from the root node down to the farthest leaf node.

      3
  9       20
        15   7
Example 1:
Input: root = [3,9,20,null,null,15,7]
Output: 3
Example 2:

Input: root = [1,null,2]
Output: 2

sol:- use recursive :-
1) recursivly find depth of left side by going left return 
2) recursivly find depth of right side by going right return 
3) return 1+Max(left,right);
so while coming back it will chcek which side high is big pick that one.