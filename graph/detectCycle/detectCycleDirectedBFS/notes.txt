Directed Graph Cycle
Given a Directed Graph with V vertices (Numbered from 0 to V-1) and E edges, check whether it contains any cycle or not.
The graph is represented as a 2D vector edges[][], where each entry edges[i] = [u, v] denotes an edge from vertex u to v.

Examples:
0 ───► 1
▲      │
│      ▼
2 ◄────┘
│
▼
3
Input: V = 4, edges[][] = [[0, 1], [1, 2], [2, 0], [2, 3]]

Output: true
Explanation: The diagram clearly shows a cycle 0 → 1 → 2 → 0
0 ───► 1
│       │
│       ▼
└────► 2 ───► 3
Input: V = 4, edges[][] = [[0, 1], [0, 2], [1, 2], [2, 3]]

Output: false
Explanation: no cycle in the graph

notes:- Now look at a cycle
0 → 1 → 2
↑       |
└───────┘

Indegree:

0 : 1
1 : 1
2 : 1

Queue:
[]
There is no node with indegree 0.
So Kahn's Algorithm cannot even start.
Processed nodes:
0
Actually, none are processed. 


imp notes:- if graph have no cycle then all nodes comes to topo becuase one by one all become indegree zero but in cycle there is
loop so it can't become zero and topo don'thave all vertext so if top arr < vertex sure one cycle.