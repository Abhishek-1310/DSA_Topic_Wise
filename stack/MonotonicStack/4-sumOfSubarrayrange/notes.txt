2104. Sum of Subarray Ranges
You are given an integer array nums. The range of a subarray of nums is the difference between the largest 
and smallest element in the subarray.
Return the sum of all subarray ranges of nums.
A subarray is a contiguous non-empty sequence of elements within an array.

Input: nums = [1,2,3]
Output: 4
Explanation: The 6 subarrays of nums are the following:
[1], range = largest - smallest = 1 - 1 = 0 
[2], range = 2 - 2 = 0
[3], range = 3 - 3 = 0
[1,2], range = 2 - 1 = 1
[2,3], range = 3 - 2 = 1
[1,2,3], range = 3 - 1 = 2
So the sum of all ranges is 0 + 0 + 0 + 1 + 1 + 2 = 4.

this is esay way to find out
how many time 1 is smaller:- 3 times then 2:- 2 times 3:- 1 times contribution=1*3+2*2+3*1=10
how many times 1 is larger 1 , 2 :- 2 times and 3 :- 3 times  contribution is=1*1+2*2+3*3=14

sum of sub array range is = sumoflarger- sumofsmaller= 14-10= 4;

notes:- 
want to find out 

if you want to find out range of all subset range means in that subset max-min so sum of all max-min in all subset
> with one for loop we can find max and min in all subset
> easy way is if we find out all max from all subset and sum it then min from all subet and sum it and subtract max-min we will get the answer 

Sum of Subarray Ranges = (Sum of Subarray MAX) − (Sum of Subarray MIN)
For every subarray:
range = max(subarray) - min(subarray)
So total:
Σ(max - min) = Σ(max) - Σ(min)

but in single loop how you will find out how many time current value is max and min 
in all subarray

there is a way there ia formula to find out how many time current elem come as minimum in subarray and max in subarray that is called contribution

1). Σ(min):-   formula for how many times current element is min in all subarray
:- for this first find out previous smaller(left) and next smaller element(right) and store the range in result and return
:- formula is= nums[i]*(i-left)*(right-i) = total contribution as minimum
this i-left right-left for this make 2 seprate helper function which store in array then formula become
total contribution=nums[i]*left[i]*right[i]
-> find out for all elements total contribution as min
Σ(min):nums[i]*left[i]*right[i]
left[] store how many time current element is smaller on left side it will find smaller and equal elemnt index then do subtract:- i-smallerElemIndex
do same for max


2) Σ(max):-  now find total contribution as max for all element 
formula= nums[i]*(i-left)*(right-i)
but here left is previoua greater element right next greater element
-> sum of all contribution as max for all elemnt 

then simple formula is:- Σ(max - min) = Σ(max) - Σ(min)

1) 
For MAX you wrote:
nums[stack[top]] > nums[i]   ❌ max is  nums[stack[top]] < nums[i] ✅ if stack top is small remove it becasue we need max value onnly 
👉 That is MIN logic, not MAX                                          so we can put in result
For MIN you wrote:
nums[stack[top]] < nums[i]   ❌ min is  nums[stack[top]] > nums[i] ✅
👉 That is MAX logic, not MIN
2) for min max in anyone we need to do = either left <= or right <= so no element miss
3) take care of loop right one start with i=n-1;


