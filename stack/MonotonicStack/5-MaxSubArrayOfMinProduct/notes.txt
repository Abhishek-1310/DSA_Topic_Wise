1856. Maximum Subarray Min-Product
The min-product of an array is equal to the minimum value in the array multiplied by the array's sum.

For example, the array [3,2,5] (minimum value is 2) has a min-product of 2 * (3+2+5) = 2 * 10 = 20.
Given an array of integers nums, return the maximum min-product of any non-empty subarray of nums. Since the answer may be large, return it modulo 109 + 7.

Note that the min-product should be maximized before performing the modulo operation. Testcases are generated such that the maximum min-product without modulo will fit in a 64-bit signed integer.

A subarray is a contiguous part of an array.

Example 1:

Input: nums = [1,2,3,2]
Output: 14
Explanation: The maximum min-product is achieved with the subarray [2,3,2] (minimum value is 2).
2 * (2+3+2) = 2 * 7 = 14.
Example 2:

Input: nums = [2,3,3,1,2]
Output: 18
Explanation: The maximum min-product is achieved with the subarray [3,3] (minimum value is 3).
3 * (3+3) = 3 * 6 = 18.
Example 3:

Input: nums = [3,1,5,6,4,2]
Output: 60
Explanation: The maximum min-product is achieved with the subarray [5,6,4] (minimum value is 4).
4 * (5+6+4) = 4 * 15 = 60.

notes;- we have to to maximize the min-product 
so in example 1 = why we didn't take 1 because which one is maximum 1*(2+1+3+2)=8 or 2*(2+3+2)= 14 
so we have to maximize it you can ask why not 2*(1+2+3+2) no from sum we have to take min mum value
 1 is min you can't multiply with 2.

what is the approch:- 
The problem asks:
min product=(minimum element in subarray)×(sum of subarray)
We need MAXIMUM of this.
so every time we will assume current element is min element how to find sum of subarray so 
we will go left and check is there any  element is less than current elem and in right becuase we assume arr[i]  is min
after find left and right we will find boundray (sub array)
to find sum of that boundary we will use prefix sum 
we will make once use many times

current element assume smallest
check left/right
But not: how many elements are maximum
Instead: how far can this minimum expand?

Expand left/right until smaller element blocks us.

:- for bigInt intilized variable with =0n  let result=0n;