Next Greater Element
You are given an array arr[] of integers, the task is to find the next greater element for each element of the array in order of their appearance in the array. Next greater element of an element in the array is the nearest element on the right which is greater than the current element.
If there does not exist next greater of current element, then next greater element for current element is -1.

Input: arr[] = [1, 3, 2, 4]
Output: [3, 4, 4, -1]
Explanation: The next larger element to 1 is 3, 3 is 4, 2 is 4 and for 4, since it doesn't exist, it is -1.

notes:- 
1) we need to start loop from right becuse we need to find nearest greater element on right
so if we traverse from right when we reach at arr[i] we already have graeter element from right in stack or arr
2) we need result array to store all greater element from right;
3) stack to store and pop element which is greater on right
4) there are three condition
>remove all smaller element campare to current ele arr[i] pop all smaller element from stack
>if there is no element in stack so store -1 in result because no element is greater on right
>after remove all element smaller from stack if stack is not empty push that element in result
5) every loop push current element in stack

default arr behaviour is stack only 
pick stack[stack.length-1];

when we will pop()-> it will pop element from top remove all element which is not greater with current value.
