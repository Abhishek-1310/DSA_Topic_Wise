Stock span problem
The stock span problem is a financial problem where we have a series of daily price quotes for a stock and we need to calculate the span of stock price for all days.
You are given an array arr[] representing daily stock prices, the stock span for the i-th day is the number of consecutive days up to day i (including day i itself) for which the price of the stock is less than or equal to the price on day i. Return the span of stock prices for each day in the given sequence.

Examples:
Input: arr[] = [100, 80, 90, 120]
Output: [1, 1, 2, 4]
Explanation: Traversing the given input span 100 is greater than equal to 100 and there are no more days behind it so the span is 1, 80 is greater than equal to 80 and smaller than 100 so the span is 1, 90 is greater than equal to 90 and 80 so the span is 2, 120 is greater than 90, 80 and 100 so the span is 4. So the output will be [1, 1, 2, 4].

find nearest greater element on left
notes:- here you can use previous greater elements if you found then subtract there index to get length (how many element is there)
1) this problem is same as previous graeter element but here instead of element we have to count number of element so 
notes:- so instead of push value in stack we have to store index
> so when we found graeter element with help of index we can find out all smaller number like current index
ex:    |greatest               |current
 arr: [   40,     10,    20,    30]    at index 3 find all smaller so find greater element so it is at index 0
          0,       1,      2,    3      count = current_index - greatest_index = 3-0= 3 element include 30 also
                                                how to find greatest_index stack[stack.length-1]
now why when stack.length === 0 have result[i]= i+1
ex:-                            |current_index 
arr: [   25,     10,    20,    30]   no greatest element found so stack become empty and you are at index 3
         0,       1,     2,     3    how many smaller element 4 right so if you will do i+1=3+1=4
         if you do result[i]=1 when stack.length==0 so at index 3 it will become wrong
i+1 is correct

pop condition stack.length > 0 && arr[stack[stack.length - 1]] <= arr[i]      stack conain index only so we need arr to extract value