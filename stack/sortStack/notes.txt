Sort a stack
Given a stack of integers st[]. Sort the stack in ascending order (smallest element at the bottom and largest at the top).
Examples:
Input: st[] = [41, 3, 32, 2, 11]
Output: [2,3,11,32,41]
Explanation: After sorting, the smallest element (2) is at the bottom and the largest element (41) is at the top.

notes:-
how to arrange , u will get stack and one element you have to put that element in correct place how to do.

we push that element in stack 
1) if:- stack is not empty and stack top <= elemt [3,41] stack top 41<=31 elem
if not else 
2) pop stack element keep it and call arrange function again pop= 41 elem = 31 stack[3]

3) arrange place 31 in stack =[3,31]

4) now pop elemnt push in stack now =[3,31,41] 
done this is
arrange 

what to do
1) Pop element
2) Sort remaining stack (this we can do with recursion )
3) Insert popped element in correct place  ( to place pop element at right place also need recursion ) so make other method for this

pop element until it become 1 then return after that insert that thing in correct place

in insert
1) when last element pop :- definetly we have to push in stack element
2) or if stack top is < = curr element then also push in stack element
else 
pop and call recursion with stack and element to place at correct position
3) then after arrange all push pop element in stack at last

sortStack([41,3,32,2,11])          ← pop rem=11
│
├─► sortStack([41,3,32,2])         ← pop rem=2
│   │
│   ├─► sortStack([41,3,32])       ← pop rem=32
│   │   │
│   │   ├─► sortStack([41,3])      ← pop rem=3
│   │   │   │
│   │   │   ├─► sortStack([41])    ← BASE CASE, return
│   │   │   │
│   │   │   └─► arrange(rem=3)     → [3, 41]
│   │   │
│   │   └─► arrange(rem=32)        → [3, 32, 41]
│   │
│   └─► arrange(rem=2)             → [2, 3, 32, 41]
│
└─► arrange(rem=11)                → [2, 3, 11, 32, 41]  ✓
