1539. Kth Missing Positive Number
Given an array arr of positive integers sorted in a strictly increasing order, and an integer k.
Return the kth positive integer that is missing from this array.

Example 1:

Input: arr = [2,3,4,7,11], k = 5
Output: 9
Explanation: The missing positive integers are [1,5,6,8,9,10,12,13,...]. The 5th missing positive integer is 9.
Example 2:

Input: arr = [1,2,3,4], k = 2
Output: 6
Explanation: The missing positive integers are [5,6,7,...]. The 2nd missing positive integer is 6.

notes:- it same as binary serach on answer but finding range is different 
:- for this problem :- Binary search happens on array indices  NOT on missing numbers.

how do we know if any number is missing or not:- if value is index+1 means no missing
how:-ex [1,2,3,4,5,6] val=4=ind+1=3+1
         0,1,2,3,4,5
what if value 3,4 missing how arr look now
[1,2,5,6] see after value shift left side
 0,1,2,3
now at index 2 it is 5 not 3   all next element shifted left side
so we have to find kth missing value so if we will find how many missing value we can find out the kth one 
so at index 2 we know we have 2 missing value and if we need 2th missing value we can do
current index + kth = 2+2 = ans is 4 check once

Main Goal = We want: kth missing number
we know to find how many missing number before current number(index); formula:- missing = arr[mid] - (mid + 1)

1) with that fromula we will get missing number with that number we will decide where we will go left or right how?

if missing < k :- still haven't reached kth missing go right  so So kth missing lies on RIGHT. l = mid + 1
else missing >= k :- kth missing already exists before or at mid = go left r = mid - 1

2) Why l + k Works

After binary search:

l = first index where missing >= k

Meaning:
before l,
there are less than k missing numbers
So kth missing shifts by l.
Hence: answer = l + k

arr = [2,3,4,7,11] k = 5
       0,1,2,3,4
when right and left cross each other that time we wil get index which tell kth missing number so it shift by left;
