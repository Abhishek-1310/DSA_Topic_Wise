Q) Minimize Max Distance to Gas Station
We have a horizontal number line. On that number line, we have gas stations at positions stations[0], stations[1],
 ..., stations[n-1]. Now, we add k more gas stations so that d, the maximum distance between adjacent gas stations,
  is minimized. We have to find the smallest possible value of d. Find the answer exactly to 6 decimal places.
Note: stations is in a strictly increasing order.

Examples:
Input: stations[] = [1, 2, 3, 4, 5], k = 2
Output: 1.00
Explanation: Since all gaps are already equal (1 unit each), adding extra stations in between does not reduce the maximum distance.
Input: stations[] = [3, 6, 12, 19, 33], k = 3
Output: 6.00 
Explanation: The largest gap is 14 (between 19 and 33). Adding 2 stations there splits it into approx 4.67. The next largest gap is 7 (between 12 and 19). Adding 1 station splits it into 3.5. Now the maximum gap left is 6.



notes:- imp:- range in decimal =  range left side min distance should be 0 if we put at same place of previous
 gas station or maximum should be we will not reduced the gap to max distance is that right range


sol:- now normal bs while(l<=r) won't work 
now we need 
while(r - l > 1e-6)
Meaning: continue until answer accurate enough

in this decimal bs  l <= r
may never stop properly. mid keep decreasing

So We Stop Based on Accuracy
Instead of saying:
search until l crosses r  = we say: =  search until both become VERY close
r - l > 1e-6
1e-6 means:

10−6  which is: 0.000001
So condition means: continue while difference is bigger than 0.000001
We keep shrinking range:

[0,10]
[0,5]
[2.5,5]
[2.5,3.75]
...
Eventually: [2.499999 , 2.500000]
Why Specifically 1e-6?
Because most coding platforms allow error like: 10^-6

After many iterations:
l = 2.499999
r = 2.500000
Now: r−l=0.000001 we will stop here


notes:- answer should lie between  min(arr) and max(arr) becasue if you will place outside of range you are 
not minizing the distance between them so you have to take ans inside range min(arr) and max(arr)

bs done left right now how to make helper function
ask is this mid possible

stations needed=⌈gap/mid​⌉−1  gap we can find out with    = current station-previous station and 
mid you will get from bs
 > and count needstation if atlast it is < = k return true

 mistake i have done in dicimal bs
 1) wrong left and right 
 this is right
let l = 0; let r = 1e8;  left and right

2) You wrote: Math.floor((l+r)/2)
Correct:  let mid = (l+r)/2;   becasue it is decimal

3) For decimal BS: r = mid l = mid    NOT: mid-1  mid+1  for bs movement
4) wrong formula [gap/mid]-1 this is right => needStat += Math.ceil(gap / mid) - 1;

