4. Median of Two Sorted Arrays
Given two sorted arrays nums1 and nums2 of size m and n respectively, return the median of the two sorted arrays.
The overall run time complexity should be O(log (m+n)).

Example 1:
Input: nums1 = [1,3], nums2 = [2]
Output: 2.00000
Explanation: merged array = [1,2,3] and median is 2.

Example 2:
Input: nums1 = [1,2], nums2 = [3,4]
Output: 2.50000
Explanation: merged array = [1,2,3,4] and median is (2 + 3) / 2 = 2.5.

imp:- In the median problem, half is always near the center of total elements, so for every mid1 in [0, len1],
 mid2 = half - mid1 usually stays within valid range.

But in the kth element problem, k can be very small or very large, so mid2 = k - mid1 can become negative or 
exceed len2, therefore we restrict the binary search range to only valid partitions.

notes:- brute force = 
make 3rd array by with given two array and return medain from 3rd array
if length is even then there were two median we will give mid of both (median1+median2)/2;
[1,2,3,4,5,6] two median m1=3 and m2=4 m1+m2/2 =  3+4/2 = 3.5 medain
else return mid of array [1,2,3,4,5] mid is 3 that is median length is odd

how to find middle of merge arr (half)= math.floor((n1+n2+1)/2);

ok but how to do it in o(1) space 
we don't need to use extra arr so what we will do is we will maintain 2 variable
elem1 and elem2 we will store the value when it reach n/2 and n/2-1 index 
we will move the pointer on both array and increse count if count reached at n/2 and n/2-1 we will store the value
arr1 = [1, 3, 5]  arr2 = [2, 4, 6] total length is 6      = n/2 = 3 and n/2-1= 2
        0  2              1  3

ok now how to do in o(logn) time
so when you have two arr seprate in 1st half both array and half seprate in other side 

for binary search:- how many we have to pick from arr1 and arr2 that thing we will get from mid as an answer
step)
1) first find how many elem from arr1 and arr2 then 
2) check that partition of array is valid or not cross check after merge if arr1 last is > arr2 first  arr2 last>arr1 first
A = [1,3,8] B = [7,9,10,11] We need: 4 elements on left side remaining on right side 
 Suppose partition: A: [1,3 | 8] B: [7 | 9,10,11]
Left side:
1,3,7  Only 3 elements.  Need 4.  Move partition.
Try:

A: [1,3,8 | ] B: [7 | 9,10,11]
Left side: 1,3,8,7    4 elements. right side  9,10,11
Now check: max(left) <= min(right)
Meaning: 8 <= 9 7 <= INF Valid partition.

for odd length max(left side) is 7 is median 7 is amswer for 
fro even length
A = [1, 3, 8, 9]  B = [7, 11, 18, 19]  total length:= 8  8/2 = 4 elements on left side 4 elements on right side
leftA <= rightB AND leftB <= rightA
 A  1 3 8 l1  r1 9        
 B      7 l2  r2 11 18 19    formula is = Max(l1,l2)+Min(r1,r2)/2
       left    right


// Always binary search on smaller array
    if(nums1.length > nums2.length){
        [nums1, nums2] = [nums2, nums1];
    }
how to apply binary serach here :- if we find out how many from arr1 we can also find out from arr2 we know
how many elemnt should be in left side  :- we have  to find out how many element in left side.

min how much we can take from arr1 zero max all we can take so 
apply binary serach from  0 to length of arr1

now how to move left and right
 A  1 3 8 l1  r1 9        
 B      7 l2  r2 11 18 19    
       left    right

if (l1>r2) right = mid -1;(reduce elem from l1)   if (l2>r1) left = mid+1 (reduce elem from l2)
