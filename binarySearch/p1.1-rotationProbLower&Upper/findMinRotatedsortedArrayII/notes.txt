154. Find Minimum in Rotated Sorted Array II
Suppose an array of length n sorted in ascending order is rotated between 1 and n times. For example, 
the array nums = [0,1,4,4,5,6,7] might become:

[4,5,6,7,0,1,4] if it was rotated 4 times.
[0,1,4,4,5,6,7] if it was rotated 7 times.
Notice that rotating an array [a[0], a[1], a[2], ..., a[n-1]] 1 time results in the array [a[n-1], a[0], a[1], a[2], ..., a[n-2]].
Given the sorted rotated array nums that may contain duplicates, return the minimum element of this array.
You must decrease the overall operation steps as much as possible.

Example 1:
Input: nums = [1,3,5]
Output: 1
Example 2:
Input: nums = [2,2,2,0,1]
Output: 0

notes:- removing ambiguity is not like Search in Rotated Array II
here we are 

We are NOT trying to know:
which side is sorted
We are trying to know:
which side contains MINIMUM
That is different.

Duplicates on left are NOT a problem by themselves.
Problem happens ONLY when:
comparison result becomes equal
because equality destroys elimination logic.

We are NOT trying to remove all duplicates.
We are trying to restore:
ability to eliminate half
Once:
nums[mid] != nums[r]
binary search works again.

Duplicates are okay.
Only equal comparison causing confusion must be removed.

Search problem:
need both boundaries → l++ and r--
Minimum problem:
need only right comparison → r--

Our entire decision system is:

if nums[mid] > nums[r]
    → minimum RIGHT
else if nums[mid] < nums[r]
    → minimum LEFT including mid

Notice:
nums[l] is never used
So ambiguity on left side does not affect anything.

[2,2,2,0,2]
Suppose:
mid=2
r=4
nums[mid]=2
nums[r]=2
Problem:
comparison failed
because:
2 == 2
We cannot decide side.
So:
r--