153. Find Minimum in Rotated Sorted Array
Suppose an array of length n sorted in ascending order is rotated between 1 and n times. For example, the array nums = [0,1,2,4,5,6,7] might become:
[4,5,6,7,0,1,2] if it was rotated 4 times.
[0,1,2,4,5,6,7] if it was rotated 7 times.
Notice that rotating an array [a[0], a[1], a[2], ..., a[n-1]] 1 time results in the array [a[n-1], a[0], a[1], a[2], ..., a[n-2]].
Given the sorted rotated array nums of unique elements, return the minimum element of this array.
You must write an algorithm that runs in O(log n) time.

Example 1:
Input: nums = [3,4,5,1,2]
Output: 1
Explanation: The original array was [1,2,3,4,5] rotated 3 times.

orinal is [1,2,3,4,5] ok how many time rotated becomw [3,4,5,1,2] 3 time first 5,4,3 so 2 become last 

sol:- 
Do NOT think: Where is minimum? how to eliminate
Think: Which half can NEVER contain minimum?

most important thing is which side is unsorted if you find you will get answer becuase everytime min present in unsorted side
if you find unsorted eliminate sorted side do this until you will find minimum and every iteration compre with minimum and change;

Minimum cannot be inside a perfectly sorted side
because elements there increase normally
and no rotation break exists there.
Minimum lies where sorted order breaks.

A perfectly sorted half CANNOT contain the minimum
(except possibly at boundary).