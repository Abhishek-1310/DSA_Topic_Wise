Search in Rotated Sorted Array II
There is an integer array nums sorted in non-decreasing order (not necessarily with distinct values).
Before being passed to your function, nums is rotated at an unknown pivot index k (0 <= k < nums.length) such that the resulting
 array is [nums[k], nums[k+1], ..., nums[n-1], nums[0], nums[1], ..., nums[k-1]] (0-indexed). For example, [0,1,2,4,4,4,5,6,6,7] 
 might be rotated at pivot index 5 and become [4,5,6,6,7,0,1,2,4,4].
Given the array nums after the rotation and an integer target, return true if target is in nums, or false if it is not in nums.

You must decrease the overall operation steps as much as possible.
Example 1:
Input: nums = [2,5,6,0,0,1,2], target = 0
Output: true
Example 2:
Input: nums = [2,5,6,0,0,1,2], target = 3
Output: false

sol:- in binary search we have to  Identify what property guarantees elimination 
same as rotated 1 but we have to eleminate duplicate by reducing window left will go right right will go left so
it will reduce duplicate window

Duplicates break certainty.
So when ambiguity happens:
nums[l] == nums[mid] == nums[r]
you CANNOT eliminate half safely.
So keep shrink until all duplicate skipped:

ambiguity == shrink

mistakes
1) we have to check  if(nums[mid] === target)  BEFORE duplicate shrinking.
2) when we are chcek which side is sorted we have to check equal condition als liek this arr[l]<=arr[mid] 
in right or left we have to check once equal so at starting only we checked;
3) when we are checking if target is in the range we don't need to be equal to mid becuse mid is already checked == taget so 
always chcek equal in side condition like  this target >= nums[l] && target < nums[mid]
and this:- target>nums[mid] && target<=nums[r]

Search problem:
need both boundaries → l++ and r--

Minimum problem:
need only right comparison → r--