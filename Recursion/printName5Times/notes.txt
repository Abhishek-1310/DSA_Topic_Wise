in output it return undefine when you will call function in console.log(Name(5)) and function will not return anything
like function just console inside function not return 
just call function like Name(5) not inside console if fun is not returing anything.

in base case don't return any value if it is not helping in calulation like in printing name no need to return any value just return is enough



“Base case is the smallest valid input where recursion stops and returns a direct answer"

Base case find
> Identify the smallest possible input

These come from math logic:
Operation	   Base Case Return
Sum (+)	           0
Product (*)	       1
Count	           0
Boolean AND	       true
Boolean OR	       false
Tree/LinkedList    null
Printing/sideEffect  return

don't return anything when Just doing work (printing, modifying array)

when you return nothing in base case go one step after valid case like if valid is 1 go till 0 if you are not returning anything.

